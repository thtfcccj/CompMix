/*********************************************************************

             延时函数标准接口-在STM32中的实现

**********************************************************************/

#include "Delay.h"

//-----------------------微秒延时函数---------------------------------
void DelayUs(unsigned short Us)
{
  for(; Us > 0; Us--){
    for(int i = (SYS_MHZ / 8); i > 0; i--);
  }
}

//-----------------------毫秒延时函数---------------------------------
void DelayMs(unsigned short Ms)
{
  for(; Ms > 0; Ms--) DelayUs(1000);
}
